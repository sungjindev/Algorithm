SELECT (SELECT SUM(SCORE) FROM HR_GRADE WHERE YEAR=2022 GROUP BY EMP_NO ORDER BY SUM(SCORE) DESC LIMIT 1) as SCORE,EMP_NO,EMP_NAME,POSITION,EMAIL FROM HR_EMPLOYEES WHERE EMP_NO = (SELECT EMP_NO FROM HR_GRADE WHERE YEAR=2022 GROUP BY EMP_NO ORDER BY SUM(SCORE) DESC LIMIT 1);