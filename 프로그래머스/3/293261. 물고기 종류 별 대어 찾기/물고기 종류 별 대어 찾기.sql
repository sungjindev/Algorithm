SELECT ID,FISH_NAME,LENGTH FROM FISH_INFO as fi INNER JOIN FISH_NAME_INFO as fni ON fi.FISH_TYPE=fni.FISH_TYPE WHERE (fi.LENGTH, fi.FISH_TYPE) IN (SELECT MAX(LENGTH),FISH_TYPE FROM FISH_INFO GROUP BY FISH_TYPE);